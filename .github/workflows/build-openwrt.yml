name: Build OpenWrt

on:
  push:
    branches:
      - main
    paths:
      - 'configs/**'
      - 'scripts/**'
      - '.github/workflows/build-openwrt.yml'
  schedule:
    # 定时编译，由仓库变量 ENABLE_SCHEDULE 控制是否实际执行
    # 在 Settings → Secrets and variables → Actions → Variables 中
    # 新建变量 ENABLE_SCHEDULE，值设为 true 即可开启，不设或设为其他值则跳过
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      target:
        description: '编译目标 (留空则编译所有)'
        required: false
        default: ''
      clean_build:
        description: '是否全量编译 (true/false)'
        required: false
        default: 'false'

env:
  REPO_URL: https://github.com/LiBwrt/openwrt-6.x
  REPO_BRANCH: main-nss
  FEEDS_CONF: feeds.conf.default
  TZ: Asia/Shanghai

jobs:
  matrix:
    name: 生成编译矩阵
    runs-on: ubuntu-22.04
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: 检查定时编译开关
        if: github.event_name == 'schedule'
        run: |
          if [ "${{ vars.ENABLE_SCHEDULE }}" != "true" ]; then
            echo "定时编译未开启，跳过本次构建"
            echo "如需开启，请在仓库 Settings → Secrets and variables → Actions → Variables 中"
            echo "新建变量 ENABLE_SCHEDULE，值设为 true"
            exit 1
          fi

      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 生成目标矩阵
        id: set-matrix
        run: |
          if [ -n "${{ github.event.inputs.target }}" ]; then
            # 手动指定目标
            echo "matrix={\"include\":[{\"config\":\"${{ github.event.inputs.target }}\"}]}" >> $GITHUB_OUTPUT
          else
            # 自动读取 configs/ 目录下所有 .config 文件
            set -o pipefail
            files=$(printf '%s\n' configs/*.config 2>/dev/null \
              | sed 's#.*/##; s/\.config$//')
            configs=$(echo "$files" \
              | grep -v '^default$' || true)
            if [ -z "$configs" ]; then
              echo 'matrix={"include":[{"config":"default"}]}' >> $GITHUB_OUTPUT
            else
              matrix=$(echo "$configs" \
                | jq -R . \
                | jq -sc '{include: [.[] | {config: .}]}')
              echo "matrix=$matrix" >> $GITHUB_OUTPUT
            fi
          fi

  build:
    name: 编译 ${{ matrix.config }}
    needs: matrix
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.matrix.outputs.matrix) }}

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 释放磁盘空间
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: 初始化编译环境
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -qq update
          sudo -E apt-get -qq full-upgrade -y
          sudo -E apt-get -qq install -y \
            ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
            bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext \
            gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 \
            libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev \
            libncurses5-dev libncursesw5-dev libreadline-dev libfuse-dev libssl-dev libtool \
            lrzsz genisoimage msmtp nano ninja-build p7zip p7zip-full patch pkgconf \
            python3 python3-pip libpython3-dev qemu-utils rsync scons squashfs-tools \
            subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean
          sudo timedatectl set-timezone "$TZ"
          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir

      - name: 克隆源码
        working-directory: /workdir
        run: |
          df -hT $PWD
          git clone --depth 1 --single-branch --branch $REPO_BRANCH $REPO_URL openwrt
          ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

      - name: 加载自定义 feeds
        run: |
          if [ -f "feeds.conf.default" ]; then
            cp feeds.conf.default openwrt/feeds.conf.default
          fi
          if [ -f "scripts/customize.sh" ]; then
            chmod +x scripts/customize.sh
            cd openwrt && $GITHUB_WORKSPACE/scripts/customize.sh
          fi

      - name: 更新并安装 feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 加载配置文件
        run: |
          CONFIG_FILE="configs/${{ matrix.config }}.config"
          if [ -f "$CONFIG_FILE" ]; then
            cp "$CONFIG_FILE" openwrt/.config
            echo "已加载配置: $CONFIG_FILE"
          elif [ -f "configs/default.config" ]; then
            cp "configs/default.config" openwrt/.config
            echo "已加载默认配置"
          else
            echo "未找到配置文件，使用默认配置"
          fi
          cd openwrt
          make defconfig

      - name: 执行自定义脚本 (编译前)
        env:
          MATRIX_CONFIG: ${{ matrix.config }}
        run: |
          # 自动扫描并按文件名顺序执行 scripts/diy-*.sh
          # 命名规则: diy-1-xxx.sh / diy-2-xxx.sh ... 数字决定执行顺序
          DIY_SCRIPTS=$(ls $GITHUB_WORKSPACE/scripts/diy-*.sh 2>/dev/null | sort)
          if [ -z "$DIY_SCRIPTS" ]; then
            echo "⚠️ 未找到任何 diy-*.sh 脚本，跳过"
          else
            for SCRIPT in $DIY_SCRIPTS; do
              echo "▶️ 执行: $(basename $SCRIPT)"
              chmod +x "$SCRIPT"
              cd /workdir/openwrt && bash "$SCRIPT"
              echo "✅ 完成: $(basename $SCRIPT)"
            done
          fi

      - name: 重新同步配置
        run: |
          cd openwrt
          make defconfig

      - name: 下载编译依赖
        id: download
        run: |
          cd openwrt
          make download -j$(nproc)
          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;

      - name: 开始编译固件
        id: compile
        run: |
          cd openwrt
          echo -e "$(nproc) 线程编译中..."
          make -j$(nproc) || make -j1 || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT
          grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' | grep -v '^ROOTFS$' > DEVICES
          if [ $(wc -l < DEVICES) -gt 1 ]; then echo "MultiDevice" > DEVICE_NAME; else cat DEVICES > DEVICE_NAME; fi
          [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
          echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

      - name: 检查磁盘空间
        if: (!cancelled())
        run: df -hT

      - name: 整理固件文件
        id: organize
        if: steps.compile.outputs.status == 'success'
        run: |
          cd openwrt/bin/targets/*/*
          # 从 manifest 获取实际编译的内核版本（比源码 grep 更准确）
          KERNEL_VER=$(find -type f -name "*.manifest" -exec grep -oP '^kernel - \K.*?(?=~|-\d+-)' {} \; | head -1)
          [ -n "$KERNEL_VER" ] && echo "KERNEL_VER=$KERNEL_VER" >> $GITHUB_ENV
          rm -rf packages
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV
          echo "status=success" >> $GITHUB_OUTPUT

      - name: 上传固件到 Artifacts
        uses: actions/upload-artifact@v4
        if: steps.organize.outputs.status == 'success'
        with:
          name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: ${{ env.FIRMWARE }}
          retention-days: 7

      - name: 生成 Release 标签
        id: tag
        if: steps.compile.outputs.status == 'success'
        run: |
          # 生成 Release 标签（北京时间）
          echo "release_tag=$(TZ=Asia/Shanghai date +"%Y.%m.%d-%H%M")-${{ matrix.config }}" >> $GITHUB_OUTPUT

          # 获取最新 commit 信息
          cd /workdir/openwrt
          COMMIT_HASH=$(git log -1 --format="%H" | cut -c1-8)
          COMMIT_AUTHOR=$(git log -1 --format="%an")
          COMMIT_DATE=$(git log -1 --format="%cd" --date=format:"%Y-%m-%d")
          COMMIT_MSG=$(git log -1 --format="%s")
          KERNEL_VER=$(grep -oP 'LINUX_KERNEL_HASH-\K[0-9]+\.[0-9]+\.[0-9]+' target/linux/generic/kernel-6.12 2>/dev/null | head -1 || echo "未知")
          echo "KERNEL_VER=$KERNEL_VER" >> $GITHUB_ENV
          cd $GITHUB_WORKSPACE

          # 读取 cfg 配置：优先读取与 config 同名的 .cfg，没有则读取 default.cfg
          CONFIG="${{ matrix.config }}"
          CFG_FILE="configs/${CONFIG}.cfg"
          [ -f "$CFG_FILE" ] || CFG_FILE="configs/default.cfg"

          DEFAULT_IP="192.168.100.1"
          DEFAULT_THEME="未设置"
          DESC="This is OpenWrt Firmware for ${CONFIG}"
          WIFI_SSID=""
          WIFI_PASSWORD=""
          NOWIFI=""
          if [ -f "$CFG_FILE" ]; then
            _ip=$(grep -m1 '^DEFAULT_IP=' "$CFG_FILE" | cut -d= -f2 | tr -d ' ')
            _theme=$(grep -m1 '^DEFAULT_THEME=' "$CFG_FILE" | cut -d= -f2 | tr -d ' ')
            _desc=$(grep -m1 '^DESC=' "$CFG_FILE" | cut -d= -f2-)
            _ssid=$(grep -m1 '^WIFI_SSID=' "$CFG_FILE" | cut -d= -f2-)
            _password=$(grep -m1 '^WIFI_PASSWORD=' "$CFG_FILE" | cut -d= -f2-)
            _nowifi=$(grep -m1 '^NOWIFI=' "$CFG_FILE" | cut -d= -f2 | tr -d ' ')
            [ -n "$_ip" ]       && DEFAULT_IP="$_ip"
            [ -n "$_theme" ]    && DEFAULT_THEME="$_theme"
            [ -n "$_desc" ]     && DESC="$_desc"
            [ -n "$_ssid" ]     && WIFI_SSID="$_ssid"
            [ -n "$_password" ] && WIFI_PASSWORD="$_password"
            [ -n "$_nowifi" ]   && NOWIFI="$_nowifi"
          fi

          # 生成 release.txt
          {
            echo "$DESC"
            echo ""
            echo "---"
            echo ""
            echo "## 📦 固件信息"
            echo ""
            echo "| 项目 | 内容 |"
            echo "|------|------|"
            echo "| 🖥️ 平台 | \`${CONFIG}\` |"
            echo "| 🔗 固件源码 | [$REPO_URL]($REPO_URL) |"
            echo "| 🌿 源码分支 | \`$REPO_BRANCH\` |"
            echo "| 🌐 默认地址 | \`${DEFAULT_IP}\` |"
            echo "| 🎨 默认主题 | \`${DEFAULT_THEME}\` |"
            echo "| 🔑 默认密码 | 无 |"
            if [ "$NOWIFI" = "true" ]; then
              echo "| 📶 WiFi | 无线版本不含 WiFi |"
            elif [ -n "$WIFI_SSID" ]; then
              echo "| 📶 WiFi 名称 | \`${WIFI_SSID}\` |"
              echo "| 🔐 WiFi 密码 | \`${WIFI_PASSWORD}\` |"
            fi
            echo ""
            echo "## 📋 固件版本"
            echo ""
            echo "| 项目 | 内容 |"
            echo "|------|------|"
            echo "| 🐧 内核版本 | \`${KERNEL_VER}\` |"
            echo "| 👤 作者 | ${COMMIT_AUTHOR} |"
            echo "| 📅 时间 | ${COMMIT_DATE} |"
            echo "| 📝 内容 | ${COMMIT_MSG} |"
            echo "| #️⃣ Hash | \`${COMMIT_HASH}\` |"
            echo ""
            echo "> 编译时间: $(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S') (北京时间)"
          } > release.txt

          echo "status=success" >> $GITHUB_OUTPUT

      - name: 发布到 Release
        uses: softprops/action-gh-release@v2
        if: steps.tag.outputs.status == 'success'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.release_tag }}
          body_path: release.txt
          files: ${{ env.FIRMWARE }}/*

      - name: 删除旧的 Releases
        uses: dev-drprasad/delete-older-releases@v0.3.4
        if: steps.compile.outputs.status == 'success'
        with:
          keep_latest: 5
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}